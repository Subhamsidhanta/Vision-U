services:
  - type: web
    name: vision-u
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0

databases:
  - name: vision-u-db
    plan: free
